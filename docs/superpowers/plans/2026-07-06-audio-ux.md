# 音声UX（チャンク再生・モデルトーク先読み・SQLiteライブラリ）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ①チャンク行ごとの🔊ボタン（押下時のみTTS生成・以降キャッシュ即再生）②モデルトークの先読み生成＋段階表示＋本文常時表示 ③生成済みモデルトークを SQLite に記録し、ライブラリ画面から本文確認・再再生できる状態にする。

**Architecture:** サーバは `bun:sqlite`（依存追加ゼロ）の薄いラッパ `db.ts` と `libraryStore`（RouteDeps 必須フィールド）を追加し、`/api/coach/model-talk` 成功時に記録、`GET /api/library/model-talks` で一覧を返す。クライアントは `api.ts` に「テキスト単位のTTS Blobキャッシュ再生」「topicId 単位のモデルトーク先読みキャッシュ」を追加し（どちらも prepPackCache と同じ in-flight Promise 共有＋失敗時削除パターン）、Warmup/4-3-2準備/Shadowing/Library の4画面から使う。

**Tech Stack:** 既存と同一（Bun + TypeScript / React + Vite）＋ `bun:sqlite`（Bun組み込み）。新規依存なし。

## Global Constraints

- サーバは 127.0.0.1:3111。**既存の HTTP 契約・既存118テストを壊さない（additive only）**。既存テストファイルの変更は「テスト追加」のみ
- RouteDeps の新フィールドは**必須**（typecheck がテストのフェイク供給を強制する）
- DB ファイルは `data/learn-english.db`（`data/.gitignore` の `*` で ignore 済み — コミットされない）。テストは `:memory:` を注入
- ログは JSONL のまま（変更しない）。**構造化された状態・履歴のみ SQLite**（本計画では model_talks テーブルのみ。SRS/メトリクスのスキーマは作らない — YAGNI）
- スキーマ管理は `CREATE TABLE IF NOT EXISTS` のみ（マイグレーション機構は作らない）
- クライアントの新規再生経路も既存規約に従う: alive-guard、アンマウント時 `stopPlayback()`、StrictMode 二重マウント対策の ref ガード
- ゲーミフィケーション制約継続（ライブラリは情報表示のみ）
- コミットは Conventional Commits（日本語）
- ゲート: サーバ `cd app && bun test` と `bun run typecheck` / クライアント `cd app/client && bun run build`

## File Structure

```
app/server/
  db.ts                        # 新規: bun:sqlite ラッパ + makeLibraryStore
  __tests__/db.test.ts         # 新規
  routes.ts                    # 変更: RouteDeps.libraryStore / handleModelTalk保存 / GET /api/library/model-talks
  index.ts                     # 変更: 実DB配線 + modelTalk が topicTitle も返す
  __tests__/routes.test.ts     # 追記: フェイク libraryStore + 新テスト
app/client/src/
  api.ts                       # 追記: ModelTalkEntry / fetchModelTalkLibrary / playTtsCached / prefetchModelTalkAudio
  screens/WarmupReadingScreen.tsx   # 変更: チャンク🔊 + unmount stopPlayback
  screens/FourThreeTwoScreen.tsx    # 変更: 準備フェーズのチャンク🔊 + モデルトーク先読み/段階表示/本文表示
  screens/ShadowingScreen.tsx       # 変更: 先読みキャッシュに統一（自動プリフェッチ）
  screens/LibraryScreen.tsx         # 新規
  screens/StartScreen.tsx           # 変更: ライブラリ導線
  App.tsx                           # 変更: library モード
```

---

### Task 1: サーバ — SQLiteライブラリとモデルトーク記録

**Files:**
- Create: `app/server/db.ts`
- Test: `app/server/__tests__/db.test.ts`
- Modify: `app/server/routes.ts`（RouteDeps・handleModelTalk・ルート追加）
- Modify: `app/server/index.ts`（realDeps 配線）
- Modify: `app/server/__tests__/routes.test.ts`（フェイク追加＋新テスト。既存テスト本体は不変）

**Interfaces:**
- Consumes: `paths.ts` の `DATA_DIR` / 既存 `routes.ts` の makeFetchHandler・parseJsonBody・json パターン
- Produces（Task 2 が依存）:
  - `type ModelTalkEntry = { id: number; createdAt: string; topicId: string; topicTitle: string; text: string }`
  - `type LibraryStore = { saveModelTalk: (e: { topicId: string; topicTitle: string; text: string }) => void; listModelTalks: (limit?: number) => ModelTalkEntry[] }`
  - `openDb(dbPath?: string): Database` / `makeLibraryStore(db: Database): LibraryStore`
  - `RouteDeps.libraryStore: LibraryStore`（必須）
  - `RouteDeps.modelTalk` の戻り値が `{ text: string; topicTitle?: string } | null` に広がる（**optional なので既存フェイクはそのまま型が通る**）
  - HTTP: `GET /api/library/model-talks` → `{ entries: ModelTalkEntry[] }`（新着順）。`POST /api/coach/model-talk` のレスポンスは従来どおり `{ text }` 固定（topicTitle は漏らさない）

- [ ] **Step 1: db.test.ts（失敗するテスト）を書く**

`app/server/__tests__/db.test.ts`:

```ts
import { describe, expect, test } from "bun:test";
import { makeLibraryStore, openDb } from "../db";

function memStore() {
  return makeLibraryStore(openDb(":memory:"));
}

describe("db / libraryStore", () => {
  test("save して list できる（新着順・スキーマ自動作成）", () => {
    const store = memStore();
    store.saveModelTalk({ topicId: "t1", topicTitle: "Zero Trust", text: "First talk." });
    store.saveModelTalk({ topicId: "t2", topicTitle: "ABAC", text: "Second talk." });
    const list = store.listModelTalks();
    expect(list).toHaveLength(2);
    expect(list[0].topicId).toBe("t2"); // 新着順
    expect(list[1]).toMatchObject({ topicId: "t1", topicTitle: "Zero Trust", text: "First talk." });
    expect(typeof list[0].id).toBe("number");
    expect(list[0].createdAt).toMatch(/^\d{4}-\d{2}-\d{2}T/);
  });

  test("同一 topicId の直近行と同一 text は重複挿入しない", () => {
    const store = memStore();
    store.saveModelTalk({ topicId: "t1", topicTitle: "Zero Trust", text: "Same talk." });
    store.saveModelTalk({ topicId: "t1", topicTitle: "Zero Trust", text: "Same talk." });
    expect(store.listModelTalks()).toHaveLength(1);
    // text が変われば挿入される
    store.saveModelTalk({ topicId: "t1", topicTitle: "Zero Trust", text: "New talk." });
    expect(store.listModelTalks()).toHaveLength(2);
  });

  test("limit が効く", () => {
    const store = memStore();
    for (let i = 0; i < 5; i++) {
      store.saveModelTalk({ topicId: `t${i}`, topicTitle: `T${i}`, text: `talk ${i}` });
    }
    expect(store.listModelTalks(2)).toHaveLength(2);
  });
});
```

- [ ] **Step 2: 失敗を確認**

Run: `cd app && bun test server/__tests__/db.test.ts`
Expected: FAIL（`../db` が存在しない）

- [ ] **Step 3: db.ts を実装**

`app/server/db.ts`:

```ts
import { Database } from "bun:sqlite";
import { mkdirSync } from "node:fs";
import path from "node:path";
import { DATA_DIR } from "./paths";

/** 構造化された状態・履歴の置き場（ログはJSONLのまま）。data/ はローカル専用（gitignore済み）。 */
export const DEFAULT_DB_PATH = path.join(DATA_DIR, "learn-english.db");

export type ModelTalkEntry = {
  id: number;
  createdAt: string;
  topicId: string;
  topicTitle: string;
  text: string;
};

export type LibraryStore = {
  saveModelTalk: (e: { topicId: string; topicTitle: string; text: string }) => void;
  listModelTalks: (limit?: number) => ModelTalkEntry[];
};

/** DBを開き、スキーマを保証する（CREATE IF NOT EXISTS のみ。マイグレーション機構はYAGNI） */
export function openDb(dbPath: string = DEFAULT_DB_PATH): Database {
  if (dbPath !== ":memory:") mkdirSync(path.dirname(dbPath), { recursive: true });
  const db = new Database(dbPath, { create: true });
  db.run("PRAGMA journal_mode = WAL");
  db.run(`CREATE TABLE IF NOT EXISTS model_talks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    created_at TEXT NOT NULL,
    topic_id TEXT NOT NULL,
    topic_title TEXT NOT NULL,
    text TEXT NOT NULL
  )`);
  return db;
}

type Row = { id: number; created_at: string; topic_id: string; topic_title: string; text: string };

export function makeLibraryStore(db: Database): LibraryStore {
  return {
    saveModelTalk(e) {
      // 連打・再訪で同じトークが無限に増えないための素朴なガード:
      // 同一トピックの直近行と本文が同じなら挿入しない
      const last = db
        .query<Pick<Row, "text">, [string]>("SELECT text FROM model_talks WHERE topic_id = ? ORDER BY id DESC LIMIT 1")
        .get(e.topicId);
      if (last && last.text === e.text) return;
      db.run(
        "INSERT INTO model_talks (created_at, topic_id, topic_title, text) VALUES (?, ?, ?, ?)",
        [new Date().toISOString(), e.topicId, e.topicTitle, e.text],
      );
    },
    listModelTalks(limit = 100) {
      const rows = db
        .query<Row, [number]>("SELECT * FROM model_talks ORDER BY id DESC LIMIT ?")
        .all(limit);
      return rows.map((r) => ({
        id: r.id,
        createdAt: r.created_at,
        topicId: r.topic_id,
        topicTitle: r.topic_title,
        text: r.text,
      }));
    },
  };
}
```

- [ ] **Step 4: グリーンを確認**

Run: `cd app && bun test server/__tests__/db.test.ts`
Expected: PASS（3 tests）

- [ ] **Step 5: routes.test.ts に失敗するテストを追記**

`app/server/__tests__/routes.test.ts` — makeTestDeps のフェイク組み立て（`...overrides` の**前**）に1フィールド追加:

```ts
    libraryStore: {
      saveModelTalk: (_e: { topicId: string; topicTitle: string; text: string }) => {},
      listModelTalks: () => [],
    },
```

ファイル末尾に describe を追加:

```ts
describe("library", () => {
  test("model-talk 成功時に libraryStore.saveModelTalk が topicTitle 付きで呼ばれ、レスポンスは {text} のみ", async () => {
    const saved: Array<{ topicId: string; topicTitle: string; text: string }> = [];
    const { deps } = makeTestDeps({
      modelTalk: async (topicId: string) =>
        topicId === "known-topic" ? { text: "model talk", topicTitle: "Known Topic" } : null,
      libraryStore: { saveModelTalk: (e) => saved.push(e), listModelTalks: () => [] },
    });
    const res = await makeFetchHandler(deps)(
      new Request("http://x/api/coach/model-talk", {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ topicId: "known-topic" }),
      }),
    );
    expect(res.status).toBe(200);
    expect(await res.json()).toEqual({ text: "model talk" }); // topicTitle を漏らさない
    expect(saved).toEqual([{ topicId: "known-topic", topicTitle: "Known Topic", text: "model talk" }]);
  });

  test("unknown topicId (404) では保存しない", async () => {
    const saved: unknown[] = [];
    const { deps } = makeTestDeps({
      libraryStore: { saveModelTalk: (e) => saved.push(e), listModelTalks: () => [] },
    });
    const res = await makeFetchHandler(deps)(
      new Request("http://x/api/coach/model-talk", {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ topicId: "nope" }),
      }),
    );
    expect(res.status).toBe(404);
    expect(saved).toHaveLength(0);
  });

  test("GET /api/library/model-talks が {entries} を返す", async () => {
    const entry = { id: 1, createdAt: "2026-07-06T00:00:00.000Z", topicId: "t1", topicTitle: "T", text: "talk" };
    const { deps } = makeTestDeps({
      libraryStore: { saveModelTalk: () => {}, listModelTalks: () => [entry] },
    });
    const res = await makeFetchHandler(deps)(new Request("http://x/api/library/model-talks"));
    expect(res.status).toBe(200);
    expect(await res.json()).toEqual({ entries: [entry] });
  });
});
```

- [ ] **Step 6: 失敗を確認**

Run: `cd app && bun test server/__tests__/routes.test.ts`
Expected: FAIL（`libraryStore` が RouteDeps に無い型エラー / 404ルート未実装）

- [ ] **Step 7: routes.ts を変更**

1. import に追加: `import type { LibraryStore } from "./db";`
2. `RouteDeps` に必須フィールドを追加し、`modelTalk` の戻り値を広げる（optional なので既存フェイクは不変で通る）:

```ts
  /** 未知の topicId は null（ルートは404を返す）。topicTitle はライブラリ記録用（レスポンスには含めない） */
  modelTalk: (topicId: string) => Promise<{ text: string; topicTitle?: string } | null>;
  /** モデルトークの記録と一覧（実体は db.ts、テストはフェイク/インメモリ） */
  libraryStore: LibraryStore;
```

3. `handleModelTalk` の成功パスを差し替え:

```ts
  const talk = await deps.modelTalk(parsed.body.topicId);
  if (!talk) return json({ error: "unknown topicId" }, 404);
  deps.libraryStore.saveModelTalk({
    topicId: parsed.body.topicId,
    topicTitle: talk.topicTitle ?? "",
    text: talk.text,
  });
  return json({ text: talk.text });
```

4. `makeFetchHandler` のルート表（`/api/progress/days` の行の近く）に追加:

```ts
      if (req.method === "GET" && url.pathname === "/api/library/model-talks")
        return json({ entries: deps.libraryStore.listModelTalks() });
```

- [ ] **Step 8: index.ts を配線**

1. import 追加: `import { makeLibraryStore, openDb } from "./db";`
2. `const realDeps: RouteDeps = {` の**前**に:

```ts
const libraryStore = makeLibraryStore(openDb());
```

3. realDeps の `modelTalk` を topicTitle 付きに変更:

```ts
  modelTalk: async (topicId) => {
    const topic = loadContent(TOPICS_DIR).find((t) => t.id === topicId);
    if (!topic) return null;
    const talk = await generateModelTalk({ topicTitle: topic.title, hints: topic.hints });
    return { text: talk.text, topicTitle: topic.title };
  },
```

4. realDeps に `libraryStore,` を追加。

- [ ] **Step 9: 全ゲートを確認**

Run: `cd app && bun test && bun run typecheck`
Expected: 118既存＋6新規 = 124 pass / typecheck 0

- [ ] **Step 10: コミット**

```bash
git add app/server/db.ts app/server/__tests__/db.test.ts app/server/routes.ts app/server/index.ts app/server/__tests__/routes.test.ts
git commit -m "feat: SQLiteライブラリを導入しモデルトークを記録・一覧APIを追加"
```

---

### Task 2: クライアント — チャンク🔊・モデルトーク先読み・ライブラリ画面

**Files:**
- Modify: `app/client/src/api.ts`
- Modify: `app/client/src/screens/WarmupReadingScreen.tsx`
- Modify: `app/client/src/screens/FourThreeTwoScreen.tsx`
- Modify: `app/client/src/screens/ShadowingScreen.tsx`
- Create: `app/client/src/screens/LibraryScreen.tsx`
- Modify: `app/client/src/screens/StartScreen.tsx`
- Modify: `app/client/src/App.tsx`

**Interfaces:**
- Consumes: Task 1 の `GET /api/library/model-talks` → `{ entries: ModelTalkEntry[] }` / 既存 `POST /api/tts` / 既存 `playBlob` `stopPlayback`（audio.ts）
- Produces: `playTtsCached(text): Promise<void>` / `prefetchModelTalkAudio(topicId, onStage?): Promise<{ text: string; blob: Blob }>` / `fetchModelTalkLibrary(): Promise<ModelTalkEntry[]>` / `type ModelTalkEntry`（サーバと同形）

- [ ] **Step 1: api.ts に追記**

ファイル末尾に追加（`playBlob` を import に追加: `import { playBlob } from "./audio";` を先頭 import 群へ）:

```ts
export type ModelTalkEntry = { id: number; createdAt: string; topicId: string; topicTitle: string; text: string };

export async function fetchModelTalkLibrary(): Promise<ModelTalkEntry[]> {
  const res = await fetch("/api/library/model-talks");
  if (!res.ok) throw new Error(`library failed: ${await extractErrorMessage(res)}`);
  return ((await res.json()) as { entries: ModelTalkEntry[] }).entries;
}

/**
 * テキスト単位のTTS Blobキャッシュ再生。初回押下時のみ /api/tts を叩き（サーバ側にも
 * テキスト単位のキャッシュがある）、以降はタブ内キャッシュから即再生。
 * in-flight Promise を共有し、失敗時はエントリを消して再試行可能にする（prepPackCache と同じパターン）。
 */
const ttsBlobCache = new Map<string, Promise<Blob>>();

export async function playTtsCached(text: string): Promise<void> {
  let p = ttsBlobCache.get(text);
  if (!p) {
    p = ttsFetch(text);
    p.catch(() => ttsBlobCache.delete(text));
    ttsBlobCache.set(text, p);
  }
  await playBlob(await p);
}

/**
 * モデルトーク（原稿テキスト→TTS Blob）の先読みキャッシュ。準備フェーズ表示時に呼び、
 * 「モデルトークを聞く」押下時には出来上がっている状態を狙う。onStage は初回生成時のみ発火する。
 */
const modelTalkCache = new Map<string, Promise<{ text: string; blob: Blob }>>();

export function prefetchModelTalkAudio(
  topicId: string,
  onStage?: (stage: "script" | "audio") => void,
): Promise<{ text: string; blob: Blob }> {
  let p = modelTalkCache.get(topicId);
  if (!p) {
    p = (async () => {
      onStage?.("script");
      const text = await fetchModelTalk(topicId);
      onStage?.("audio");
      const blob = await ttsFetch(text);
      return { text, blob };
    })();
    p.catch(() => modelTalkCache.delete(topicId));
    modelTalkCache.set(topicId, p);
  }
  return p;
}
```

- [ ] **Step 2: WarmupReadingScreen にチャンク🔊を追加**

`app/client/src/screens/WarmupReadingScreen.tsx` を以下に置き換え（変更点: import、`playingIdx` 状態、チャンク行の🔊ボタン、unmount `stopPlayback`、行内エラー表示）:

```tsx
import { useEffect, useRef, useState } from "react";
import { fetchPrepPack, playTtsCached, type ContentItem, type PrepPack } from "../api";
import { stopPlayback } from "../audio";

type State = "loading" | "ready" | "error";

/**
 * セッション冒頭の低負荷な音読ウォームアップ。今日のトピックの表現チャンクと骨組みを
 * 声に出して読むだけ（録音・採点なし）。この後の4/3/2で同じ素材を使う下地作り。
 */
export function WarmupReadingScreen(props: { topic: ContentItem }) {
  const [state, setState] = useState<State>("loading");
  const [prep, setPrep] = useState<PrepPack | null>(null);
  const [errorMsg, setErrorMsg] = useState("");
  const [playErr, setPlayErr] = useState("");
  const [playingIdx, setPlayingIdx] = useState<number | null>(null);
  const aliveRef = useRef(true);
  const fetchedRef = useRef(false); // StrictMode の二重マウントで prep を二重フェッチしない

  useEffect(() => {
    aliveRef.current = true;
    if (!fetchedRef.current) {
      fetchedRef.current = true;
      load();
    }
    return () => {
      aliveRef.current = false;
      stopPlayback();
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  async function load() {
    setState("loading");
    setErrorMsg("");
    try {
      const pack = await fetchPrepPack(props.topic.id);
      if (!aliveRef.current) return;
      setPrep(pack);
      setState("ready");
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
      setState("error");
    }
  }

  async function playChunk(i: number, text: string) {
    setPlayErr("");
    setPlayingIdx(i);
    try {
      await playTtsCached(text);
    } catch (err) {
      if (!aliveRef.current) return;
      setPlayErr(err instanceof Error ? err.message : String(err));
    } finally {
      if (aliveRef.current) setPlayingIdx(null);
    }
  }

  const chunks = prep?.chunks.filter((c) => typeof c.en === "string" && c.en) ?? [];

  return (
    <div>
      <p style={{ color: "#666" }}>
        声に出して読みましょう（各フレーズ2回ずつ）。🔊でお手本を聞けます。このあとの 4/3/2 で実際に使います。
      </p>
      {state === "loading" && <p>コーチが表現チャンクを用意しています…</p>}
      {state === "error" && (
        <div>
          <p style={{ color: "crimson" }}>
            {errorMsg} <button onClick={load}>再試行</button>
          </p>
          {props.topic.hints.length > 0 && (
            <div>
              <h4>代わりにこちらを声に出して読みましょう</h4>
              <ul>
                {props.topic.hints.map((h, i) => (
                  <li key={i}>{h}</li>
                ))}
              </ul>
            </div>
          )}
        </div>
      )}
      {state === "ready" && prep && (
        <div>
          {chunks.length > 0 && (
            <ul>
              {chunks.map((c, i) => (
                <li key={i} style={{ marginBottom: "0.4rem" }}>
                  <button
                    onClick={() => playChunk(i, c.en)}
                    disabled={playingIdx !== null}
                    style={{ marginRight: "0.5rem", cursor: "pointer" }}
                    aria-label={`「${c.en}」を再生`}
                  >
                    {playingIdx === i ? "…" : "🔊"}
                  </button>
                  <strong>{c.en}</strong>
                  {c.ja && <div style={{ color: "#666", marginLeft: "2.2rem" }}>{c.ja}</div>}
                </li>
              ))}
            </ul>
          )}
          {playErr && <p style={{ color: "crimson" }}>{playErr}</p>}
          {prep.outline.length > 0 && (
            <div>
              <h4>今日の話の骨組み</h4>
              <ol>
                {prep.outline.map((o, i) => (
                  <li key={i}>{o}</li>
                ))}
              </ol>
            </div>
          )}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: FourThreeTwoScreen の準備フェーズを更新**

`app/client/src/screens/FourThreeTwoScreen.tsx` — 変更は準備フェーズ関連のみ。ラウンド/AE/イベント送信ロジックには触らない。

1. import を更新: `fetchModelTalk, ttsFetch` を外し `playTtsCached, prefetchModelTalkAudio` を追加:

```ts
import {
  fetchAeFeedback, fetchPrepPack, playTtsCached, prefetchModelTalkAudio, sendSessionEvent, sttUpload,
  type AeFeedback, type ContentItem, type PrepPack,
} from "../api";
```

2. 状態を差し替え: `const [modelPlaying, setModelPlaying] = useState(false);` を以下に置換:

```ts
  type ModelState = "script" | "audio" | "ready" | "playing" | "error";
  const [modelState, setModelState] = useState<ModelState>("script");
  const [modelText, setModelText] = useState("");
```

3. マウント effect（`prepFetchedRef` ガード内、`loadPrep(); prepTimer.start();` の直後）に先読みを追加:

```ts
      prefetchModelTalkAudio(props.topic.id, (stage) => {
        if (aliveRef.current) setModelState(stage);
      })
        .then(({ text }) => {
          if (!aliveRef.current) return;
          setModelText(text);
          setModelState("ready");
        })
        .catch(() => {
          if (aliveRef.current) setModelState("error");
        });
```

4. `playModelTalk` を差し替え:

```ts
  async function playModelTalk() {
    setErrorMsg("");
    setModelState("playing");
    try {
      const { text, blob } = await prefetchModelTalkAudio(props.topic.id);
      if (!aliveRef.current) return;
      setModelText(text);
      const { playBlob } = await import("../audio");
      await playBlob(blob);
      if (aliveRef.current) setModelState("ready");
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
      setModelState("error");
    }
  }
```

   ※動的 import を避け、既存の static import（`playBlob` は既に `../audio` から import 済み）をそのまま使ってよい。その場合この行は不要。

5. 準備フェーズ JSX の「使える表現」リストの各行に Warmup と同じ🔊ボタンを付ける（`playingIdx` 状態を Warmup と同様に追加し、`playChunk(i, c.en)` は `playTtsCached` を使う。コードは Step 2 と同形なので省略せず実装者が同じパターンを書くこと）:

```tsx
                      <li key={i}>
                        <button
                          onClick={() => playChunk(i, c.en)}
                          disabled={playingIdx !== null}
                          style={{ marginRight: "0.5rem", cursor: "pointer" }}
                          aria-label={`「${c.en}」を再生`}
                        >
                          {playingIdx === i ? "…" : "🔊"}
                        </button>
                        <strong>{c.en}</strong>
                        {c.ja && <span style={{ color: "#666" }}> — {c.ja}</span>}
                      </li>
```

   `playChunk` は Warmup と同じ実装（`playErr` ではなく既存 `errorMsg` を使ってよい）。

6. モデルトークボタンと本文表示を差し替え（準備フェーズ JSX の該当 `<p>` を置換）:

```tsx
        <p>
          <button
            onClick={playModelTalk}
            disabled={modelState === "script" || modelState === "audio" || modelState === "playing"}
            style={{ padding: "0.6rem 1.2rem" }}
          >
            {modelState === "script" && "✍ 原稿を作成中…"}
            {modelState === "audio" && "🎙 音声を生成中…"}
            {modelState === "ready" && "🎧 モデルトークを聞く（任意）"}
            {modelState === "playing" && "🔊 再生中…"}
            {modelState === "error" && "🎧 モデルトーク（再試行）"}
          </button>{" "}
          <button onClick={() => startRound(0)} style={{ padding: "0.6rem 1.2rem" }}>
            Round 1 を始める（{minLabel(roundsSec[0])}）→
          </button>
        </p>
        {modelText && (
          <details open style={{ marginTop: "0.5rem" }}>
            <summary style={{ cursor: "pointer", color: "#666" }}>モデルトーク本文</summary>
            <p style={{ whiteSpace: "pre-wrap", lineHeight: 1.8 }}>{modelText}</p>
          </details>
        )}
```

   ※ `modelState === "error"` のボタン押下 `playModelTalk` はキャッシュが削除済みのため再生成から再試行される。

- [ ] **Step 4: ShadowingScreen を先読みキャッシュに統一**

`app/client/src/screens/ShadowingScreen.tsx` を以下に置き換え（自動プリフェッチ化。「生成する」ボタンは廃止し、マウント時に生成開始）:

```tsx
import { useEffect, useRef, useState } from "react";
import { prefetchModelTalkAudio, type ContentItem } from "../api";
import { playBlob, stopPlayback } from "../audio";

type State = "script" | "audio" | "ready" | "playing" | "error";

/** モデルトークをTTSで聞きながら重ねて音読するシャドーイングブロック（知覚ドリル） */
export function ShadowingScreen(props: { topic: ContentItem }) {
  const [state, setState] = useState<State>("script");
  const [text, setText] = useState("");
  const [audioBlob, setAudioBlob] = useState<Blob | null>(null);
  const [errorMsg, setErrorMsg] = useState("");
  const aliveRef = useRef(true);
  const fetchedRef = useRef(false);

  useEffect(() => {
    aliveRef.current = true;
    if (!fetchedRef.current) {
      fetchedRef.current = true;
      prepare();
    }
    return () => {
      aliveRef.current = false;
      stopPlayback();
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  async function prepare() {
    setErrorMsg("");
    setState("script");
    try {
      const { text: t, blob } = await prefetchModelTalkAudio(props.topic.id, (stage) => {
        if (aliveRef.current) setState(stage);
      });
      if (!aliveRef.current) return;
      setText(t);
      setAudioBlob(blob);
      setState("ready");
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
      setState("error");
    }
  }

  async function play() {
    if (!audioBlob) return;
    setState("playing");
    try {
      await playBlob(audioBlob);
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
    }
    if (aliveRef.current) setState("ready");
  }

  return (
    <div>
      <p style={{ color: "#666" }}>
        音声に少し遅れてかぶせるように声に出して繰り返します（シャドーイング）。まず1回聞くだけでもOK。
      </p>
      {state === "script" && <p>✍ コーチがモデルトークを書いています…</p>}
      {state === "audio" && <p>🎙 音声を生成しています…</p>}
      {state === "error" && (
        <p style={{ color: "crimson" }}>
          {errorMsg} <button onClick={prepare}>再試行</button>
        </p>
      )}
      {(state === "ready" || state === "playing") && (
        <div>
          <button onClick={play} disabled={state === "playing"} style={{ padding: "0.6rem 1.2rem" }}>
            {state === "playing" ? "🔊 再生中…" : "▶ 再生（何度でも）"}
          </button>
          <p style={{ whiteSpace: "pre-wrap", lineHeight: 1.8 }}>{text}</p>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 5: LibraryScreen を新規作成**

`app/client/src/screens/LibraryScreen.tsx`:

```tsx
import { useEffect, useRef, useState } from "react";
import { fetchModelTalkLibrary, playTtsCached, type ModelTalkEntry } from "../api";
import { stopPlayback } from "../audio";

type State = "loading" | "ready" | "error";

/** 生成済みモデルトークの一覧（情報表示のみ）。本文確認と再再生ができる。 */
export function LibraryScreen() {
  const [state, setState] = useState<State>("loading");
  const [entries, setEntries] = useState<ModelTalkEntry[]>([]);
  const [errorMsg, setErrorMsg] = useState("");
  const [playingId, setPlayingId] = useState<number | null>(null);
  const aliveRef = useRef(true);
  const fetchedRef = useRef(false);

  useEffect(() => {
    aliveRef.current = true;
    if (!fetchedRef.current) {
      fetchedRef.current = true;
      load();
    }
    return () => {
      aliveRef.current = false;
      stopPlayback();
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  async function load() {
    setState("loading");
    setErrorMsg("");
    try {
      const list = await fetchModelTalkLibrary();
      if (!aliveRef.current) return;
      setEntries(list);
      setState("ready");
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
      setState("error");
    }
  }

  async function play(entry: ModelTalkEntry) {
    setErrorMsg("");
    setPlayingId(entry.id);
    try {
      await playTtsCached(entry.text);
    } catch (err) {
      if (!aliveRef.current) return;
      setErrorMsg(err instanceof Error ? err.message : String(err));
    } finally {
      if (aliveRef.current) setPlayingId(null);
    }
  }

  return (
    <div>
      <h3>📚 モデルトークライブラリ</h3>
      {state === "loading" && <p>読み込み中…</p>}
      {state === "error" && (
        <p style={{ color: "crimson" }}>
          {errorMsg} <button onClick={load}>再試行</button>
        </p>
      )}
      {state === "ready" && entries.length === 0 && (
        <p style={{ color: "#666" }}>
          まだありません。4/3/2 の準備やシャドーイングでモデルトークを生成すると、ここに残ります。
        </p>
      )}
      {state === "ready" &&
        entries.map((e) => (
          <div key={e.id} style={{ borderTop: "1px solid #ddd", padding: "0.8rem 0" }}>
            <p style={{ margin: 0 }}>
              <button
                onClick={() => play(e)}
                disabled={playingId !== null}
                style={{ marginRight: "0.5rem", cursor: "pointer" }}
              >
                {playingId === e.id ? "🔊 再生中…" : "▶"}
              </button>
              <strong>{e.topicTitle || e.topicId}</strong>{" "}
              <span style={{ color: "#888", fontSize: "0.85rem" }}>{e.createdAt.slice(0, 10)}</span>
            </p>
            <details>
              <summary style={{ cursor: "pointer", color: "#666" }}>本文</summary>
              <p style={{ whiteSpace: "pre-wrap", lineHeight: 1.8 }}>{e.text}</p>
            </details>
          </div>
        ))}
      {state === "ready" && errorMsg && <p style={{ color: "crimson" }}>{errorMsg}</p>}
    </div>
  );
}
```

- [ ] **Step 6: StartScreen と App.tsx に導線を追加**

`StartScreen.tsx`:
1. `StartSelection` に `| { type: "library" }` を追加。
2. 「自由会話のみ」ボタンの直後に:

```tsx
      <button style={btn} onClick={() => props.onSelect({ type: "library" })}>📚 ライブラリ（モデルトークの復習）</button>
```

`App.tsx`:
1. import に `LibraryScreen` を追加: `import { LibraryScreen } from "./screens/LibraryScreen";`
2. `type Mode` に `| { kind: "library" }` を追加。
3. `onSelect` に分岐を追加（`free` 分岐の直後）:

```ts
    else if (sel.type === "library") setMode({ kind: "library" });
```

4. JSX に追加（`mode.kind === "free"` の行の直後）:

```tsx
      {mode.kind === "library" && <LibraryScreen />}
```

- [ ] **Step 7: ゲートを確認**

Run: `cd app/client && bun run build`
Expected: tsc + vite ともに成功

Run: `cd app && bun test`
Expected: 124 pass（サーバは Task 1 から不変）

- [ ] **Step 8: 実プロキシスモーク**

APIサーバ（3111）と Vite（5173）が起動している状態で:

```bash
curl -s http://localhost:5173/api/library/model-talks
```

Expected: `{"entries":[...]}`（過去に model-talk を生成していれば1件以上、なければ `{"entries":[]}`）

ブラウザ確認はオーナーの受け入れテスト（🔊ボタン・先読み済みモデルトークの即再生・ライブラリ画面）に委ねる旨をレポートに記す。

- [ ] **Step 9: コミット**

```bash
git add app/client/src/
git commit -m "feat: チャンク音声再生・モデルトーク先読みと本文表示・ライブラリ画面を追加"
```

---

## Self-Review 結果（プラン作成時に実施済み）

- **スペック網羅**: ユーザー要望4点（🔊ボタン=Task2 Step2/3、押下時のみ生成→以後何度でも=playTtsCached＋サーバTTSキャッシュ、モデルトーク本文表示=Task2 Step3、履歴からの再再生=ライブラリ）をすべてカバー。SQLite導入判断（状態はDB・ログはJSONL）も反映
- **型整合**: `ModelTalkEntry` はサーバ（db.ts）とクライアント（api.ts）で同形。`RouteDeps.modelTalk` の戻り値拡張は optional フィールドのため既存テストのフェイクがそのまま型を通る（Step 5 で新フェイクを追加するのは libraryStore のみ）
- **プレースホルダ**: なし。全ステップに実コード
- **注意点**: FourThreeTwoScreen Step 3-4 の `playBlob` は既存 static import を使う（動的 import の行は不要と明記済み）。ShadowingScreen の自動プリフェッチ化は挙動変更（ボタン押下→自動）だがブロックの目的上妥当、レビューで確認する
