import { Card } from "../ui/Card";

export function ChunkPlaceholderScreen() {
  return (
    <Card>
      <p className="text-muted">このブロックは M3（チャンクSRS）で本実装されます。</p>
      <p className="text-muted">今日は: 最近の仕事で使った/使いたかった英語表現を思い出して、声に出して3回言ってみてください。</p>
    </Card>
  );
}
